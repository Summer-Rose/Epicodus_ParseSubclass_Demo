<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin" tools:context=".MainActivity">

    <EditText
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/kittenNameEditText"
        android:hint="Kitten Name"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="45dp"/>

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Create Kitten"
        android:id="@+id/createKittenButton"
        android:layout_below="@+id/ownerEditText"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="33dp"/>

    <EditText
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/birthdayEditText"
        android:hint="Birthday (Ex. 01-21-2007)"
        android:paddingTop="20dp"
        android:layout_below="@+id/kittenNameEditText"
        android:layout_centerHorizontal="true"/>

    <EditText
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/ownerEditText"
        android:hint="Owner Name"
        android:paddingTop="20dp"
        android:layout_below="@+id/birthdayEditText"
        android:layout_centerHorizontal="true"/>

</RelativeLayout>
